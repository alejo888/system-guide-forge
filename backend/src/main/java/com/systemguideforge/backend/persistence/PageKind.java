package com.systemguideforge.backend.persistence;

/** Identifies pages that need special treatment beyond an ordinary crawled page. Only login pages are marked today. */
public enum PageKind { LOGIN }
